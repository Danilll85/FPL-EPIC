import styled from "@emotion/styled";

const SubmitFeedbackWrapper = styled.div`
  padding: 2rem 4rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
`;

const Header = styled.h1`
  font-size: 3rem;
`;

export { SubmitFeedbackWrapper, Header };
